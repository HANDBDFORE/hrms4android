<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/login_background"
    android:orientation="vertical" >

    <!-- 标题 -->

    <TextView
        android:id="@+id/activity_login_textview_title"
        style="@android:style/TextAppearance.Large"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="5dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="30dp"
        android:gravity="center"
        android:text="@string/activity_login_prompt_title" />

    <!-- 账户 -->

    <LinearLayout
        style="@style/login_input_container"
        android:layout_marginTop="30dp"
        android:background="@drawable/container_no_corner" > <!--  -->

        <TextView
            style="@style/login_prompt"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/activity_login_prompt_username" />

        <EditText
            android:id="@+id/activity_login_textedit_username"
            style="@style/login_edittext"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:imeOptions="actionNext"
            android:inputType="text" />
    </LinearLayout>

    <!-- 密码 -->

    <LinearLayout
        style="@style/login_input_container"
        android:layout_marginTop="-1dp"
        android:background="@drawable/container_no_corner" > <!-- android:background="@drawable/container_down_corner" -->

        <TextView
            style="@style/login_prompt"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/activity_login_prompt_password" />

        <EditText
            android:id="@+id/activity_login_textedit_password"
            style="@style/login_edittext"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:imeOptions="actionGo"
            android:inputType="textPassword"
            android:singleLine="true" />
    </LinearLayout>

    <!-- 登录 -->

    <Button
        android:id="@+id/activity_login_textedit_loginButton"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="-1dp"
        android:background="@drawable/btn_login"
        android:text="@string/activity_login_loginbutton_text" />

</LinearLayout>